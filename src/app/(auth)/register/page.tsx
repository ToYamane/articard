import { RegisterForm } from '@/components/auth';

export const metadata = {
  title: '新規登録 | Articard',
  description: 'Articardに新規登録',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
