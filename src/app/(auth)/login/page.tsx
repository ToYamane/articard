import { LoginForm } from '@/components/auth';

export const metadata = {
  title: 'ログイン | Articard',
  description: 'Articardにログイン',
};

export default function LoginPage() {
  return <LoginForm />;
}
