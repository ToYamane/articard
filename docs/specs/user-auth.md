# 05. 認証・ユーザー管理

## 5.1 機能概要

Firebase Authenticationを使用したユーザー認証とプロフィール管理。

## 5.2 認証方式

### 対応認証方式

| 方式              | 優先度 | 備考                 |
| ----------------- | ------ | -------------------- |
| メール/パスワード | 必須   | 標準認証             |
| Google OAuth      | 必須   | 最も利用率が高い想定 |
| Apple OAuth       | 必須   | iOS App Store要件    |

### 認証フロー

```
┌─────────────────────────────────────────────────────┐
│                    未認証状態                        │
│  ┌─────────────────────────────────────────────┐   │
│  │             ログイン / 新規登録               │   │
│  │                                             │   │
│  │  ┌─────────────────────────────────────┐   │   │
│  │  │        Googleでログイン              │   │   │
│  │  └─────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────┐   │   │
│  │  │        Appleでログイン               │   │   │
│  │  └─────────────────────────────────────┘   │   │
│  │                                             │   │
│  │  ─────────────── または ───────────────    │   │
│  │                                             │   │
│  │  メールアドレス: [________________]         │   │
│  │  パスワード:     [________________]         │   │
│  │                                             │   │
│  │  ┌──────────────┐  ┌──────────────┐       │   │
│  │  │   ログイン    │  │  新規登録    │       │   │
│  │  └──────────────┘  └──────────────┘       │   │
│  │                                             │   │
│  │  パスワードを忘れた方はこちら               │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

## 5.3 新規登録フロー

### メール/パスワード登録

```
メールアドレス入力
    │
    ▼
パスワード入力（8文字以上）
    │
    ▼
Firebase Authentication でアカウント作成
    │
    ▼
メール確認リンク送信
    │
    ▼
┌─────────────────┐
│ ニックネーム設定 │ ← 初回ログイン時に必須
└─────────────────┘
    │
    ▼
アカウント作成完了
```

### OAuth登録

```
Google/Apple ログインボタン
    │
    ▼
OAuth認証画面（外部）
    │
    ▼
Firebase Authentication でアカウント作成/リンク
    │
    ▼
┌─────────────────┐
│ ニックネーム設定 │ ← 初回ログイン時に必須
└─────────────────┘
    │
    ▼
アカウント作成完了
```

## 5.4 ユーザープロフィール

### プロフィール項目

| 項目           | 必須 | 編集可 | 仕様               |
| -------------- | ---- | ------ | ------------------ |
| ニックネーム   | ✅   | ✅     | 2-20文字、重複可   |
| メールアドレス | ✅   | ❌     | Firebase管理       |
| 登録日         | 自動 | ❌     | 自動記録           |
| 認証プロバイダ | 自動 | ❌     | email/google/apple |

### ニックネーム仕様

```typescript
interface NicknameValidation {
  minLength: 2;
  maxLength: 20;
  pattern: /^[a-zA-Z0-9ぁ-んァ-ヶー一-龠々]+$/;  // 英数字、ひらがな、カタカナ、漢字
  forbiddenWords: string[];  // NGワードリスト
}
```

## 5.5 ユーザーデータ構造

### Firebase Auth User

```typescript
// Firebase が管理
interface FirebaseUser {
  uid: string;
  email: string;
  emailVerified: boolean;
  providerData: ProviderInfo[];
  creationTime: string;
  lastSignInTime: string;
}
```

### アプリケーション User

```typescript
// Cloud SQL で管理
interface User {
  id: string; // Firebase UID と同一
  nickname: string;

  // 通貨
  coinBalance: number; // 永続コイン残高
  dailyFreeCoins: number; // 当日無料コイン

  // サブスクリプション
  isPremium: boolean;
  premiumTier: 'plus' | 'premium' | null;
  premiumExpiresAt: Date | null;

  // メタデータ
  createdAt: Date;
  updatedAt: Date;
  lastActiveAt: Date;
}
```

## 5.6 セッション管理

### Firebase Auth セッション

```typescript
// クライアント側
import { getAuth, onAuthStateChanged } from 'firebase/auth';

const auth = getAuth();

onAuthStateChanged(auth, (user) => {
  if (user) {
    // ログイン状態
    const idToken = await user.getIdToken();
    // API リクエストに idToken を含める
  } else {
    // 未ログイン状態
  }
});
```

### API 認証

```typescript
// サーバー側（API Routes）
import { getAuth } from 'firebase-admin/auth';

const verifyToken = async (req: Request): Promise<DecodedIdToken> => {
  const authHeader = req.headers.get('authorization');
  const token = authHeader?.replace('Bearer ', '');

  if (!token) {
    throw new UnauthorizedError('No token provided');
  }

  const decodedToken = await getAuth().verifyIdToken(token);
  return decodedToken;
};
```

## 5.7 アカウント設定画面

```
┌────────────────────────────────────────┐
│ アカウント設定                          │
├────────────────────────────────────────┤
│                                        │
│ プロフィール                           │
│ ┌────────────────────────────────────┐ │
│ │ ニックネーム                        │ │
│ │ [カードマスター太郎    ] [変更]    │ │
│ └────────────────────────────────────┘ │
│                                        │
│ アカウント情報                         │
│ ┌────────────────────────────────────┐ │
│ │ メールアドレス                      │ │
│ │ example@email.com                  │ │
│ │                                    │ │
│ │ 登録日                             │ │
│ │ 2025年1月1日                       │ │
│ │                                    │ │
│ │ 認証方法                           │ │
│ │ 🔵 Google                          │ │
│ └────────────────────────────────────┘ │
│                                        │
│ コイン残高                             │
│ ┌────────────────────────────────────┐ │
│ │ 🟢 60 (無料) 🟡 120 (永続)  [購入] │ │
│ └────────────────────────────────────┘ │
│                                        │
│ サブスクリプション                     │
│ ┌────────────────────────────────────┐ │
│ │ 現在: 無料プラン                    │ │
│ │ [プラス/プレミアムにアップグレード] │ │
│ └────────────────────────────────────┘ │
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ 🚪 ログアウト                       │ │
│ └────────────────────────────────────┘ │
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ 🗑️ アカウント削除                   │ │
│ └────────────────────────────────────┘ │
│                                        │
└────────────────────────────────────────┘
```

## 5.8 アカウント削除

### 削除フロー

```
削除ボタンタップ
    │
    ▼
確認ダイアログ表示
「本当に削除しますか？
 この操作は取り消せません。
 すべてのカードと記事が削除されます。」
    │
    ▼
再認証（セキュリティ）
    │
    ▼
┌─────────────────┐
│ データ削除処理   │
│ 1. カード画像削除（Cloud Storage）
│ 2. カードデータ削除（Cloud SQL）
│ 3. 記事データ削除（Cloud SQL）
│ 4. ユーザーデータ削除（Cloud SQL）
│ 5. Firebase Auth削除
└─────────────────┘
    │
    ▼
削除完了・トップページへリダイレクト
```

## 5.9 パスワードリセット

### リセットフロー

```
「パスワードを忘れた」リンク
    │
    ▼
メールアドレス入力
    │
    ▼
Firebase Auth でリセットメール送信
    │
    ▼
ユーザーがメール内リンクをクリック
    │
    ▼
新しいパスワード設定画面（Firebase提供）
    │
    ▼
パスワード更新完了
```

## 5.10 コインシステム

### 概要

| 項目       | 仕様                                     |
| ---------- | ---------------------------------------- |
| 通貨名     | コイン                                   |
| 種別       | 無料コイン（日次リセット）、永続コイン   |
| 無料コイン | 毎日90コイン（プランにより変動）         |
| 有効期限   | 無料コインは当日限り、永続コインは無期限 |

### 取得方法

- 毎日の無料コイン付与（90～300コイン/日、プランにより変動）
- チャレンジモード達成報酬（B/A/Sランク達成時、各30コイン）
- サブスクリプション初回ボーナス
- コインパッケージ購入

### 使用用途

- カード生成（30コイン/枚）
- チャレンジ追加回数（無料回数超過後、10コイン/回）

詳細は [コインシステム](./coin-system.md) を参照。

## 5.11 サブスクリプションプラン

### プラン比較

| 機能             | 無料   | プラス (¥980/月) | プレミアム (¥2,980/月) |
| ---------------- | ------ | ---------------- | ---------------------- |
| 毎日コイン       | 90     | 150              | 300                    |
| 永続コイン付与   | -      | 初回300          | 初回900                |
| チャレンジ無料   | 3回/日 | 10回/日          | 無制限                 |
| バッチカード生成 | -      | 最大10枚同時     | 最大10枚同時           |

### 課金実装

- Web: Stripe Checkout（実装済み）
- iOS: App Store In-App Purchase（予定）
- Android: Google Play Billing（予定）

詳細は [Stripe連携ガイド](../guides/stripe-integration.md) を参照。

## 5.12 エラーハンドリング

| エラー           | ユーザー表示                                         | 対応               |
| ---------------- | ---------------------------------------------------- | ------------------ |
| メール重複       | 「このメールアドレスは既に登録されています」         | ログイン画面へ誘導 |
| パスワード弱     | 「パスワードは8文字以上で設定してください」          | 再入力を促す       |
| OAuth失敗        | 「認証に失敗しました。再度お試しください」           | リトライ           |
| セッション切れ   | 「セッションが切れました。再度ログインしてください」 | ログイン画面へ     |
| ニックネーム不正 | 「ニックネームは2-20文字で入力してください」         | 再入力を促す       |
